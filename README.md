# Clean Code Bot

CLI tool that refactors and documents code using LLMs (Groq).

## Setup

```bash
pip install -r requirements.txt
export GROQ_API_KEY=your_api_key

## Setup (recommended)

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt